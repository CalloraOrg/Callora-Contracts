[package]
name = "callora-limits"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
callora-helpers = { path = "../helpers" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
callora-settlement = { path = "../settlement" }
callora-revenue-pool = { path = "../revenue_pool" }
