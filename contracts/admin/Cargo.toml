[package]
name = "callora-admin"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
soroban-sdk = { workspace = true }
callora-topics = { path = "../topics" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
callora-topics = { path = "../topics" }
